name: Build SERFCN Tweak

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: brew install make ldid xz

    - name: Setup Theos
      run: |
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        rm -rf $HOME/theos/sdks
        git clone --depth 1 https://github.com/theos/sdks.git $HOME/theos/sdks
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV

    - name: Download libAPIClient.a
      run: |
        mkdir -p API
        curl -L -o API/libAPIClient.a https://github.com/seadnossi-netizen/SERFCN/releases/download/v1.0.0-lib/libAPIClient.a

    - name: Build Tweak
      run: |
        export THEOS=$HOME/theos
        make clean
        make package FINALPACKAGE=1

    - name: Upload .deb
      uses: actions/upload-artifact@v4
      with:
        name: SERFCN-deb
        path: packages/*.deb

    - name: Upload .dylib
      uses: actions/upload-artifact@v4
      with:
        name: SERFCN-dylib
        path: .theos/obj/debug/SERFCN.dylib
